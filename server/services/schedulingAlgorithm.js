/**
 * ===================================================================================================
 * Scheduling Algorithm (자동 배정 알고리즘)
 * ===================================================================================================
 *
 * 설명: 멤버들의 선호시간을 기반으로 자동 시간 배정
 *
 * 주요 알고리즘:
 * - 보통 모드: 선택지 적은 멤버 우선 배정
 * - 우선순위 모드: 먼저 들어온 멤버 우선
 *
 * 배정 프로세스:
 * 1. 각 멤버의 가용 슬롯 수 계산
 * 2. 가용 슬롯 적은 순으로 정렬
 * 3. 순서대로 빈 슬롯에 배정
 * 4. 충돌 방지
 *
 * 관련 파일:
 * - server/controllers/coordinationController.js
 * - server/models/room.js
 *
 * ===================================================================================================
 */

/**
 * 스케줄링 알고리즘 모듈 (리다이렉트)
 *
 * 이 파일은 모듈화된 schedulingAlgorithm 폴더의 index.js로 리다이렉트합니다.
 *
 * 모듈화된 구조:
 * - schedulingAlgorithm/
 *   - constants/     (상수 정의)
 *   - utils/         (유틸리티 함수)
 *   - validators/    (검증 함수)
 *   - helpers/       (헬퍼 함수)
 *   - services/      (서비스 함수)
 *   - index.js       (메인 진입점)
 */

module.exports = require('./schedulingAlgorithm/index');

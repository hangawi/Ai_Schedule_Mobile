/**
 * ===================================================================================================
 * Modal.js - 모든 모달의 기본이 되는 재사용 가능한 모달 래퍼(Wrapper) 컴포넌트
 * ===================================================================================================
 *
 * 📍 위치: 프론트엔드 > client/src/components/modals/Modal.js
 *
 * 🎯 주요 기능:
 *    - 화면 전체를 덮는 반투명 오버레이(overlay)를 제공.
 *    - `isOpen` prop을 통해 모달의 열림/닫힘 상태를 제어.
 *    - 모달의 기본 레이아웃(제목, 닫기 버튼, 콘텐츠 영역)을 제공.
 *    - `children` prop을 통해 모달 내부에 어떠한 콘텐츠든 렌더링할 수 있도록 지원.
 *
 * 🔗 연결된 파일:
 *    - 이 컴포넌트를 직접 사용하는 파일은 많지 않으나, 다른 대부분의 모달 컴포넌트들이 이 컴포넌트를 기반으로
 *      만들어지거나 유사한 구조를 가집니다. (예: CustomAlertModal, EventFormModal 등)
 *
 * 💡 UI 위치:
 *    - 이 컴포넌트는 특정 위치에 고정되지 않고, 필요한 모든 곳에서 팝업 형태로 화면 최상단에 표시됩니다.
 *
 * ✏️ 수정 가이드:
 *    - 모든 모달의 기본적인 스타일(배경 오버레이, 모달의 둥근 모서리, 그림자 등)을 변경하려면 이 파일을 수정해야 합니다.
 *    - 모달이 열리고 닫히는 애니메이션을 추가하려면 이 파일의 최상위 `div`에 transition 관련 CSS 클래스를 추가할 수 있습니다.
 *
 * 📝 참고사항:
 *    - 이 컴포넌트는 가장 기본적인 형태의 모달이므로, 복잡한 상태 관리 로직은 포함하지 않습니다.
 *    - `isOpen` prop이 `false`이면 `null`을 반환하여 아무것도 렌더링하지 않습니다.
 *
 * ===================================================================================================
 */

import React from 'react';

/**
 * Modal
 * @description 애플리케이션 전반에서 사용될 수 있는 기본 모달 컴포넌트.
 * @param {object} props - 컴포넌트 props
 * @param {boolean} props.isOpen - 모달이 열려있는지 여부.
 * @param {function} props.onClose - 모달을 닫기 위해 호출되는 함수 (주로 'x' 버튼 클릭 시).
 * @param {string} props.title - 모달의 제목.
 * @param {React.ReactNode} props.children - 모달 내부에 표시될 콘텐츠.
 * @returns {JSX.Element|null} 모달이 열려있으면 JSX를, 아니면 null을 반환.
 */
const Modal = ({ isOpen, onClose, title, children }) => {
  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full z-50 flex justify-center items-center">
      <div className="relative p-5 border w-80 shadow-lg rounded-md bg-white">
        <div className="flex justify-between items-center mb-4">
          <h3 className="text-lg font-bold text-gray-900">{title}</h3>
          <button onClick={onClose} className="text-gray-400 hover:text-gray-600 text-2xl leading-none font-semibold">&times;</button>
        </div>
        <div className="mt-2">
          {children}
        </div>
      </div>
    </div>
  );
};

export default Modal;

/**
 * ===================================================================================================
 * apiConstants.js - API 요청에 사용되는 기본 URL을 정의하는 상수 파일
 * ===================================================================================================
 *
 * 📍 위치: 프론트엔드 > client/src/hooks/useChat/constants/apiConstants.js
 *
 * 🎯 주요 기능:
 *    - 백엔드 API 서버의 기본 URL을 정의합니다.
 *    - 환경 변수를 사용하여 개발 및 배포 환경에 따라 유연하게 URL을 설정합니다.
 *
 * 🔗 연결된 파일:
 *    - client/src/hooks/useChat/handlers/enhancedIntentHandlers.js - API 요청을 보내는 핸들러 파일
 *    - package.json - 스크립트 실행 시 환경 변수를 설정할 수 있음
 *
 * 💡 UI 위치:
 *    - 직접적인 UI 요소는 없지만, 애플리케이션의 모든 API 통신에 간접적으로 영향을 미칩니다.
 *
 * ✏️ 수정 가이드:
 *    - 이 파일을 수정하면: 애플리케이션의 모든 API 요청이 다른 엔드포인트로 향하게 됩니다.
 *    - API 기본 URL 변경: `process.env.REACT_APP_API_BASE_URL` 환경 변수 또는 직접 'http://localhost:5000' 값을 수정합니다.
 *
 * 📝 참고사항:
 *    - `REACT_APP_API_BASE_URL` 환경 변수가 설정되지 않은 경우, 기본값으로 'http://localhost:5000'이 사용됩니다.
 *    - 배포 환경에서는 이 환경 변수를 올바르게 설정하는 것이 중요합니다.
 *
 * ===================================================================================================
 */

export const API_BASE_URL = process.env.REACT_APP_API_BASE_URL || 'http://localhost:5000';
